<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="VesselScheduleMgtDBDAOModifyVskVslPortSkdByClptSeqUSQL">
			<desc><![CDATA[VVD의 Virtual Port 삭제 후 Clpt Seq 를 순차적으로 다시 생성.

* History
* 2011.02.22 진마리아 CHM-201108456-01 사업계획용 스케줄 정보 노출 대응을 위한 시스템 업데이트건]]></desc>
			<sql><![CDATA[
UPDATE  VSK_BUD_VSL_PORT_SKD A
SET CLPT_SEQ = (
                SELECT RNUM
                FROM (
                        SELECT RID, ROWNUM AS RNUM
                        FROM (
                                SELECT ROWID AS RID FROM VSK_BUD_VSL_PORT_SKD
                                WHERE VSL_CD = @[vsl_cd]
                                AND SKD_VOY_NO = @[skd_voy_no]
                                AND SKD_DIR_CD = @[skd_dir_cd]
                                ORDER BY CLPT_SEQ
                             ) 
                     )
                 WHERE RID = A.ROWID
                )
WHERE VSL_CD = @[vsl_cd]
AND SKD_VOY_NO = @[skd_voy_no]
AND SKD_DIR_CD = @[skd_dir_cd]			]]></sql>
			<params>
				<param name="vsl_cd" type="12" value="" out="N"/>
				<param name="skd_voy_no" type="12" value="" out="N"/>
				<param name="skd_dir_cd" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
