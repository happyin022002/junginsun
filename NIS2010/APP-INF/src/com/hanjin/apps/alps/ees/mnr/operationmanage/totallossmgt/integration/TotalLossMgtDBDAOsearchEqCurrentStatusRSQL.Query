<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="TotalLossMgtDBDAOsearchEqCurrentStatusRSQL">
			<desc><![CDATA[Total Loss 제각처리 취소시, 현재의 EQ Status를 확인한다.
만약 이미 활성화된 장비라면 작업을 진행하지 않는다.]]></desc>
			<sql><![CDATA[
#if (${eq_kind_cd} == 'U')
SELECT CRNT_YD_CD AS YD_CD, ACIAC_DIV_CD FROM MST_CONTAINER
WHERE CNTR_NO = @[eq_no]
#else
SELECT CRNT_YD_CD AS YD_CD, ACIAC_DIV_CD FROM CGM_EQUIPMENT
WHERE EQ_NO = @[eq_no]
#end			]]></sql>
			<params>
				<param name="eq_no" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
