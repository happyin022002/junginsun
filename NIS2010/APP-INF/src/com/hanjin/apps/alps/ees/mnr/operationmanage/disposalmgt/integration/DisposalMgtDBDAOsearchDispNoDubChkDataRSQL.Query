<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="DisposalMgtDBDAOsearchDispNoDubChkDataRSQL">
			<desc><![CDATA[DESC Enter..]]></desc>
			<sql><![CDATA[
SELECT MAX(MDD.DISP_NO) AS DISP_NO
FROM MNR_DISP_DTL MDD,MNR_DISP_HDR MDH
WHERE MDD.DISP_NO = MDH.DISP_NO
AND MDH.DISP_STS_CD <> 'HD'
#if (${eq_no_list} != '')	
    AND MDD.EQ_NO IN (
    	#foreach ($user_eq_no IN ${eqNos})
			#if($velocityCount < $eqNos.size())
            	'$user_eq_no',
            #else
                '$user_eq_no'
            #end
		#end                      
	)
#end			]]></sql>
			<params>
			</params>
		</query>
	</querys>
</sqls>
