<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="GeneralCodeSearchMgtDBDAOsearchOfcComboCodeListDataRSQL">
			<desc><![CDATA[------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
searchOfcComboCodeListData
2014-03-31 by Jonghee HAN 선반영 CSR Ticket : CHM-201429703, Title : ALPS MNR-Disposal Request 시, Office Code 변경에 따른 Approval Office 변경 요청, 부산지역 조직통합으로 Disposal Request시 Approval Office 선택을 위한 PUSSC Office Code 추가
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------]]></desc>
			<sql><![CDATA[
SELECT   A.OFC_CD AS CD_ID
       , A.OFC_CD AS CD_DESC
FROM     MNR_OFC_GEN_INFO A
WHERE    1 = 1
AND      A.UPPR_OFC_CD  = @[searchkey]

#if (${searchcon} == 'DISP')
AND      A.MNR_GRP_TP_CD = 'DSP'
#else
AND      A.MNR_GRP_TP_CD = 'OFC'
AND      A.COST_CD       = 'RPROFC'
#end

#if (${searchcon} == 'DISP')
UNION ALL
SELECT   CASE WHEN @[searchkey] IN ('PUSBO','PUSSC','TYOSC','OSASO') THEN ( SELECT MNR_CD_ID FROM MNR_GEN_CD WHERE PRNT_CD_ID = 'HOOFC' ) ELSE @[searchkey] END AS CD_ID
       , CASE WHEN @[searchkey] IN ('PUSBO','PUSSC','TYOSC','OSASO') THEN ( SELECT MNR_CD_ID FROM MNR_GEN_CD WHERE PRNT_CD_ID = 'HOOFC' ) ELSE @[searchkey] END AS CD_DESC 
FROM DUAL
#else
UNION ALL
SELECT   @[searchkey] AS CD_ID
       , @[searchkey] AS CD_DESC
FROM     DUAL
#end			]]></sql>
			<params>
				<param name="searchkey" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
