<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="SpaceAllocationManageDBDAOSearchSpaceAllocationControlFlagListRSQL">
			<desc><![CDATA[- VVD 별 Control Option 을 Port별,type size별,  weight별로  관리한다
   port별 관리는 office,pol,pod가 있는데 pod로 선택되어지면 ctrl_port_flag = 'Y'가 되고  ctrl_lvl_cd = 'D'가 된다.
   pod를 제외하고 office나 pol로 선택되어지면  ctrl_port_flag = 'N'으로 되고, ctrl_lvl_cd = 'O'(office), ctrl_lvl_cd = 'L' (pol)로 셋팅되어진다.
- CHOI.Y.S - [프로젝트] Ticket ID : CHM-201004171 - 53FT 관련 필드 추가
[CHM-201322502-01] SPC 프로젝트 - 성수기 선복운영 개선을 위한 T/F추진
2015.07.24 Arie [CHM-201537010] Control Option Management 및 VVD별 EDIT기능 보완  - Sync/Desync 빼고 Yield Group, Fixed Edit 추가]]></desc>
			<sql><![CDATA[
SELECT NVL(MIN(T.CTRL_LVL_CD)      , 'L') AS POL_POD,
       NVL(MIN(T.CTRL_40FT_HC_FLG) , 'N') AS HC40   ,
       NVL(MIN(T.CTRL_45FT_HC_FLG) , 'N') AS HC45   ,
       NVL(MIN(T.CTRL_53FT_FLG)    , 'N') AS HC53   ,
       NVL(MIN(T.CTRL_RF_FLG)      , 'N') AS REEFER ,
       NVL(MIN(T.CTRL_WGT_FLG)     , 'N') AS WEIGHT ,
       NVL(MIN(T.CTRL_SPC_FLG)     , 'N') AS VOLUME ,
       NVL(MIN(DECODE(T.REP_TRD_CD, @[trade], T.ACCT_GRP_CTRL_FLG)), 'N') AS ACCT, --smp
       -- 2014.08.01 control option 추가
       NVL(MIN(T.CTRL_D2_FLG)      , 'N') AS D2     ,
       NVL(MIN(T.CTRL_D4_FLG)      , 'N') AS D4     ,
       NVL(MIN(T.CTRL_RD_FLG)      , 'N') AS RD     ,
       NVL(MIN(T.CTRL_USA_SVC_MOD_FLG), 'N') AS USA ,
       NVL(MIN(T.CTRL_ACCT_FLG), 'N') AS ACCOUNT    , -- account/commodity
       NVL(MIN(DECODE(T.CTRL_ECC_FLG, 'Y', 'E', DECODE(T.CTRL_ECC_GRP_FLG, 'Y', 'G', 'N'))), 'N') AS CTRL_DEST,
       NVL(MIN(T.CTRL_DEST_LVL_CD), 'N') AS CTRL_DEST_LVL
       , NVL(MIN(T.CTRL_FX_RT_FLG), 'N') AS CTRL_FX_RT_FLG
  FROM SPC_ALOC_CTRL_OPT T
 WHERE T.RLANE_CD   = @[lane]
   AND T.DIR_CD     = @[bound]
   AND T.VSL_CD     = SUBSTR(@[vvd], 0, 4)
   AND T.SKD_VOY_NO = SUBSTR(@[vvd], 5, 4)
   AND T.SKD_DIR_CD = SUBSTR(@[vvd], 9)			]]></sql>
			<params>
				<param name="trade" type="12" value="" out="N"/>
				<param name="lane" type="12" value="" out="N"/>
				<param name="bound" type="12" value="" out="N"/>
				<param name="vvd" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
