<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="BLDocumentationBLDBDAOsearchPkcMeasWgtSiRSQL">
			<desc><![CDATA[searchPkcMeasWgtSi]]></desc>
			<sql><![CDATA[
SELECT SUM(PCK_QTY) PCK_QTY
        , SUM(MEAS_QTY) MEAS_QTY
        , SUM(ACT_WGT) ACT_WGT
  FROM BKG_BL_DOC
 WHERE BKG_NO IN (
#foreach( ${bkg_no} in ${bkg_no_list}) 
	#if($velocityCount < $bkg_no_list.size()) '$bkg_no', #else '$bkg_no' #end
#end
)			]]></sql>
			<params>
			</params>
		</query>
	</querys>
</sqls>
