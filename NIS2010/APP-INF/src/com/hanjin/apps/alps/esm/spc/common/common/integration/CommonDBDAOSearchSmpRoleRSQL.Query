<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="CommonDBDAOSearchSmpRoleRSQL">
			<desc><![CDATA[Login user의 Office Kind와 Level을 조회합니다.
[CHM-201322502-01] SPC 프로젝트 - 성수기 선복운영 개선을 위한 T/F추진]]></desc>
			<sql><![CDATA[
SELECT (SELECT DECODE(COUNT(*),0,'N','Y')
          FROM COM_USR_ROLE_MTCH
         WHERE USR_ID = T.USR_ID
           AND USR_ROLE_CD = 'SPC01'
       ) AS SPC_01_YN,
       (SELECT DECODE(COUNT(*),0,'N','Y')
          FROM COM_USR_ROLE_MTCH
         WHERE USR_ID = T.USR_ID
           AND USR_ROLE_CD = 'SPC08'
       ) AS SPC_08_YN,
       (SELECT DECODE(COUNT(*),0,'N','Y')
          FROM COM_USR_ROLE_MTCH
         WHERE USR_ID = T.USR_ID
           AND USR_ROLE_CD = 'SPC09'
       ) AS SPC_09_YN
FROM COM_USER T
WHERE USR_ID = @[usr_id]			]]></sql>
			<params>
				<param name="usr_id" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
