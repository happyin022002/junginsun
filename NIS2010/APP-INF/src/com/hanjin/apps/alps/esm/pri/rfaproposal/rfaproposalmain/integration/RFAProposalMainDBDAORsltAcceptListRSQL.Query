<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="RFAProposalMainDBDAORsltAcceptListRSQL">
			<desc><![CDATA[accept data  조회

* History
2015.10.28 [CHM-201538236] RFA module 승인 절차 간소화 및 기능 개선 Request by SELCMU/김현경
                 전체 정보와 Scope의 상태 중 승인 안된 부분 정보 취득.]]></desc>
			<sql><![CDATA[
SELECT PROP_NO
FROM   (SELECT 'MN' PROP_NO
        FROM   PRI_RP_AMDT_SMRY
        WHERE  PROP_NO  = @[prop_no]
        AND    AMDT_SEQ = @[amdt_seq]
        AND    ACPT_FLG = 'N'
        AND    AMDT_FLG = 'Y'
        AND    ROWNUM = 1
        UNION ALL
        SELECT 'SCP' PROP_NO
        FROM   PRI_RP_SCP_AMDT_SMRY
        WHERE  PROP_NO  = @[prop_no]
        AND    AMDT_SEQ = @[amdt_seq]
        AND    ACPT_FLG = 'N'
        AND    AMDT_FLG = 'Y'
        AND    ROWNUM = 1)
WHERE  ROWNUM = 1			]]></sql>
			<params>
				<param name="prop_no" type="12" value="" out="N"/>
				<param name="amdt_seq" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
