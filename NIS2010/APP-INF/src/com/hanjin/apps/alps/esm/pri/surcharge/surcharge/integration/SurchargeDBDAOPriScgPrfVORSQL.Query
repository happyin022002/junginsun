<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="SurchargeDBDAOPriScgPrfVORSQL">
			<desc><![CDATA[2013.03.07 전윤주 [CHM-201323465]  Reefer condition type 추가
2013.04.17 전윤주 [CHM-201324203]  Contract Date 추가
2013.10.01 전윤주 [CHM-201326927] MDM rating flag와 연계하여 Auto 항목 추가
2013.10.01 전윤주 [CHM-201326929] BL Printing시 숨길 수 있는 Hide 항목 추가
2014.03.20 전윤주 [CHM-201429456] Food Grade 항목 추가
2014.04.10 전윤주 [CHM-201429656] State code 항목 추가  
2015.04.10 전지예 [CHM-201535041] Arrival Date 항목 추가]]></desc>
			<sql><![CDATA[
SELECT 
	SVC_SCP_CD
,	CHG_CD
,	FLT_PCT_TP_CD
,	PCT_BSE_CD
,	SUB_TRD_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SUB_TRD_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SUB_TRD_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS SUB_TRD_CNT
,	SLAN_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN VSL_SLAN_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN VSL_SLAN_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL )AS VSL_SLAN_CNT
,	POR_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN POR_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN POR_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS POR_DEF_CNT
,	POL_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN POL_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN POL_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS POL_DEF_CNT
,	POD_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN POD_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN POD_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS POD_DEF_CNT
,	DEL_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN DEL_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN DEL_DEF_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS DEL_DEF_CNT
,	IMDG_CLSS_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SCG_IMDG_CLSS_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SCG_IMDG_CLSS_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS SCG_IMDG_CLSS_CNT
,	TS_PORT_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN TS_PORT_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN TS_PORT_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS TS_PORT_CNT
,	TML_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN TML_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN TML_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS TML_CNT
,	TRNS_MOD_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN ORG_TRSP_MOD_CD IS NULL AND DEST_TRSP_MOD_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN ORG_TRSP_MOD_CD IS NULL AND DEST_TRSP_MOD_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS TRNS_MOD_CNT
,	USA_SVC_MOD_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN USA_SVC_MOD_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN USA_SVC_MOD_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS USA_SVC_MOD_CNT
,	RCV_DE_TERM_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN PRC_RCV_TERM_CD IS NULL AND PRC_DE_TERM_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN PRC_RCV_TERM_CD IS NULL AND PRC_DE_TERM_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS RCV_DE_TERM_CNT
,	HNGR_BAR_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN PRC_HNGR_BAR_TP_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN PRC_HNGR_BAR_TP_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS HNGR_BAR_CNT
,	DIR_CALL_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN DIR_CALL_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN DIR_CALL_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS DIR_CALL_CNT
,	CGO_WGT_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN MIN_CGO_WGT IS NULL AND MAX_CGO_WGT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN MIN_CGO_WGT IS NULL AND MAX_CGO_WGT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS CGO_WGT_CNT
,	CMDT_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN CMDT_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN CMDT_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS CMDT_CNT
,	GRI_CMDT_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SCG_GRP_CMDT_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SCG_GRP_CMDT_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS GRI_CMDT_CNT
,	SOC_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SOC_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SOC_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS SOC_CNT
,   IO_GA_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN IO_GA_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN IO_GA_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS IG_GA_CNT
,   CNL_TZ_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN CNL_TZ_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN CNL_TZ_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS CNL_TZ_CNT
,   CRTE_DY_KNT_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SCG_CRTE_DY_KNT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SCG_CRTE_DY_KNT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS SCG_CRTE_DY_KNT_CNT
,   PRD_CRTE_TP_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SCG_PRD_TP_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SCG_PRD_TP_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS SCG_PRD_TP_CD_CNT        
,   PRD_CRTE_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN SCG_PRD_CRTE_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN SCG_PRD_CRTE_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS SCG_PRD_CRTE_CD_CNT      
,   PSA_NO_MNG_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN PSA_NO IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN PSA_NO IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS PSA_NO_CNT 
,   RC_AIR_COND_TP_USE_FLG
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN RC_AIR_COND_TP_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN RC_AIR_COND_TP_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS RC_AIR_COND_TP_CNT 
,   CTRT_DT_USE_FLG  
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN CTRT_DT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN CTRT_DT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS CTRT_DT_CNT  
,   ACT_RAT_USE_FLG  
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN ACT_RAT_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN ACT_RAT_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS ACT_RAT_CNT  
,   PRN_HDN_USE_FLG  
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN PRN_HDN_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN PRN_HDN_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS PRN_HDN_CNT 
,   FD_GRD_USE_FLG  
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN FD_GRD_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN FD_GRD_FLG IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS FD_GRD_CNT    
,   STE_USE_FLG  
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN STE_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN STE_CD IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS STE_CD_CNT   
,   ARR_DT_USE_FLG  
,   (SELECT (SELECT NVL(SUM(CASE
                   WHEN ARR_DT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') +
        (SELECT NVL(SUM(CASE
                   WHEN ARR_DT IS NULL THEN 0
                   ELSE 1
                END), 0)
       FROM PRI_SCG_RQST_RT 
      WHERE SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
        AND CHG_CD = @[chg_cd]
        AND DELT_FLG = 'N') FROM DUAL) AS ARR_DT_CNT 
,	CRE_USR_ID
,	TO_CHAR(CRE_DT, 'YYYY-MM-DD') AS CRE_DT
,	UPD_USR_ID
,	TO_CHAR(UPD_DT, 'YYYY-MM-DD') AS UPD_DT
FROM PRI_SCG_PRF
WHERE	SVC_SCP_CD IN (SELECT SVC_SCP_CD FROM PRI_SCG_TRD_SVC_SCP_MAPG WHERE TRD_CD = @[trd_cd])
AND	CHG_CD = @[chg_cd]			]]></sql>
			<params>
				<param name="trd_cd" type="12" value="" out="N"/>
				<param name="chg_cd" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
