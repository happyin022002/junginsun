<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="SCProposalMainDBDAOPriSpHdrPreFixChkVORSQL">
			<desc><![CDATA[이전
사용자의 off_cd 로
mdm_organization 검색 하여 loc_cd를 찾고
loc_cd로 mdm_location을 검색하여 sconti_cd를 찾는다.
찾은 sconti_cd로 PRI_SC_PFX_MAPG 의 sconti_cd와 
scope_cd로 join하여 pre_fix를 가져온다.
SELECT SVC_SCP_CD
FROM   PRI_SC_PFX_MAPG
WHERE  SCONTI_CD = (SELECT SCONTI_CD
                    FROM   MDM_LOCATION
                    WHERE  LOC_CD = (SELECT LOC_CD
                                     FROM   MDM_ORGANIZATION
                                     WHERE  OFC_CD = @[ofc_cd]))
AND    SC_PFX_CD = @[pre_fix]
AND ROWNUM = 1
변경 PRI_SC_PFX 테이블에서만 CHECK한다.]]></desc>
			<sql><![CDATA[
SELECT SC_PFX_CD SVC_SCP_CD
FROM PRI_SC_PFX
WHERE SC_PFX_CD = @[pre_fix]
AND DELT_FLG ='N'
AND ROWNUM = 1			]]></sql>
			<params>
				<param name="pre_fix" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
