<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="AGNCommApprovalDBDAOModifyCsrHeaderApInvHdrInfoUSQL">
			<desc><![CDATA[AP_INV_HDR 데이터를 업데이트한다.

20141017 수정 : approved_yn 가 Y 이면 기존Approval로직 사용 즉, Approved 단계임
pproved_yn 가 N 이면 기존Approval로직 사용안함 즉, GW결재라인요청상태임 > Approval 단계임
]]></desc>
			<sql><![CDATA[
UPDATE AP_INV_HDR
   SET IF_DT      = SYSDATE,
	   ACCT_XCH_RT_YRMON = SUBSTR(GL_DT,1,6),
       CSR_USD_AMT = AP_COM_GET_USD_XCH_AMT_FNC(CSR_CURR_CD, CSR_AMT, SUBSTR(GL_DT,1,6)),
#if (${approved_yn} == 'Y')
  	   IF_ERR_RSN = 'Approval Request!'
#else 
  	   IF_ERR_RSN = 'Requesting Approval'
#end
 WHERE CSR_NO     = @[csr_no]			]]></sql>
			<params>
				<param name="csr_no" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
