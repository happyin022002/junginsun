<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="SCReportDBDAORsltPriMotTrfRtMaxRtSeqRSQL">
			<desc><![CDATA[PRI_MOT_TRF_RT 의 SVC_SCP_CD, MOT_TRF_SEQ 별 Maximum RT_SEQ 조회
하나의 값을 조회하여 여러개의 List 를 대상으로 사용하므로 Loop 안에서 Increment 연산을 수행하여 사용하므로 
현재의 Maximum RT_SEQ 값을 조회한다.]]></desc>
			<sql><![CDATA[
SELECT  NVL (   (   SELECT /*+ INDEX_DESC(A XPKPRI_MOT_TRF_RT) */
                       	    RT_SEQ
                    FROM	PRI_MOT_TRF_RT A
                    WHERE	A.SVC_SCP_CD = @[svc_scp_cd]
                    AND     A.MOT_TRF_SEQ = @[mot_trf_seq]
                    AND		ROWNUM = 1 ), 0 ) AS MAX_RT_SEQ
FROM    DUAL			]]></sql>
			<params>
				<param name="svc_scp_cd" type="12" value="" out="N"/>
				<param name="mot_trf_seq" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
