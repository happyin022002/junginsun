<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="NetworkCostDBDAOSearchTrunkIPCPricingRSQL">
			<desc><![CDATA[History ----------------------------
2010.10.22 이행지 [CHM-201006375-01][MAS] Trunk IPC와 Ocean간 내부거래 신규 추가 
                                                       - Trunk IPC Internal Pricing 화면 Retrieve 버튼적용 쿼리]]></desc>
			<sql><![CDATA[
SELECT	
	    DECODE(A.TRD_CD,NULL,'I','R') IBFLAG
    ,	DECODE(A.TRD_CD,NULL,@[f_cost_yrmon],A.COST_YRMON) COST_YRMON
    ,	ROWNUM NO
    ,	B.TRD_CD
    ,	DECODE(A.TRD_CD,NULL,0,A.INTER_PRC_UC_AMT) INTER_PRC_UC_AMT
  FROM	MAS_INTER_PRC_UT_COST A
		,(
			SELECT	INTG_CD_VAL_CTNT AS TRD_CD
			  FROM	COM_INTG_CD A,
			        COM_INTG_CD_DTL B
			 WHERE	A.INTG_CD_ID      =  B.INTG_CD_ID
			   AND	A.INTG_CD_ID      = 'CD02765'
			   AND	A.INTG_CD_USE_FLG = 'Y'
		 ) B
 WHERE	A.TRD_CD(+)     = B.TRD_CD
   AND	A.COST_YRMON(+)	= @[f_cost_yrmon]
   AND	A.DELT_FLG(+)   = 'N'			]]></sql>
			<params>
				<param name="f_cost_yrmon" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
