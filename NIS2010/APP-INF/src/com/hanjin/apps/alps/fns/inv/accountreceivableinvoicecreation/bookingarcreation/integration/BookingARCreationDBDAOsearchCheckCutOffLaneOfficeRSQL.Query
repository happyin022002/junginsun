<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="BookingARCreationDBDAOsearchCheckCutOffLaneOfficeRSQL">
			<desc><![CDATA[MDM에서 삭제된 Office 경우 CutOffLane에 변경된 AR Office가 등록되어있는지 Check
MDM 삭제된 Office의 AR Office를 CutOffLane에서 새로 구하는 Query임]]></desc>
			<sql><![CDATA[
SELECT NEW_AR_OFC_CD
FROM   INV_CUT_OFF_LANE
WHERE  OLD_AR_OFC_CD = @[ar_ofc_cd]
AND    IO_BND_CD = @[io_bnd_cd]
AND    SLAN_CD = 'ALL'
AND    PORT_CD = 'ALL'
AND    APLY_DT <= TO_CHAR(SYSDATE,'YYYYMMDD')
AND    EXISTS (SELECT 'X'
               FROM   MDM_ORGANIZATION
               WHERE  OFC_CD = @[ar_ofc_cd]
               AND    DELT_FLG = 'Y')
AND    ROWNUM = 1			]]></sql>
			<params>
				<param name="ar_ofc_cd" type="12" value="" out="N"/>
				<param name="io_bnd_cd" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
