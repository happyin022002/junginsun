<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="TonnageTaxFillingDBDAOSearchUpdateRSQL">
			<desc><![CDATA[운항중인 모든 선박(피더선박 제외)들의 최신 Update Date 조회한다.]]></desc>
			<sql><![CDATA[
SELECT to_char(MAX(UPD_DT), 'YYYY-MM-DD HH:MM') UPD_DT
FROM TOT_VVD_STL_AMT
WHERE  STL_YRMON BETWEEN @[year]||'01' AND @[year]||'12'			]]></sql>
			<params>
				<param name="year" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
