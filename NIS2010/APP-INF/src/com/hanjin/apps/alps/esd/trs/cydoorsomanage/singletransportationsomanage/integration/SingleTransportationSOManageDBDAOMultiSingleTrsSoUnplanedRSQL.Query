<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="SingleTransportationSOManageDBDAOMultiSingleTrsSoUnplanedRSQL">
			<desc><![CDATA[Unpland SO일 경우 COP_NO, COST_ACT_GRP_SEQ, FROM NODE, TO NODE, Unmathced 여부를 체 같이
체크하여 이 정보가 중복될 경우엔 S/O를 발행할 수 없다.]]></desc>
			<sql><![CDATA[
SELECT
     ('COP No:'||COP_NO||', BKG No:'||BKG_NO||', CNTR No:'||EQ_NO) CHK_UNIT
 FROM
     TRS_TRSP_SVC_ORD
WHERE COP_NO = @[cop_no]
  AND FM_NOD_CD = @[fm_nod_cd]
  AND TO_NOD_CD = @[to_nod_cd]
AND DELT_FLG                 = 'N'
AND NVL(TRSP_FRST_FLG,'N')   = 'N'
AND NVL(UPLN_SO_FLG,'N')     = 'Y' 			]]></sql>
			<params>
				<param name="cop_no" type="12" value="" out="N"/>
				<param name="fm_nod_cd" type="12" value="" out="N"/>
				<param name="to_nod_cd" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
