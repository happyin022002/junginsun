<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="COPSearchDBDAOSearchSOExistenceRSQL">
			<desc><![CDATA[SO 존재여부를 확인한다. SO 가 실제 존재할 경우에면 COP change 의 validation 을 수행한다.]]></desc>
			<sql><![CDATA[
SELECT '1' FROM TRS_TRSP_SVC_ORD WHERE COP_NO = @[cop_no] AND NVL(TRSP_FRST_FLG, 'N') = 'N'
AND NVL(DELT_FLG, 'N') = 'N' AND CGO_TP_CD = 'F' AND TRSP_SO_TP_CD != 'S' AND NVL(RPLN_UMCH_FLG, 'N') = 'N'
UNION ALL
SELECT '2' FROM TRS_TRSP_RAIL_BIL_ORD WHERE COP_NO = @[cop_no] AND NVL(TRSP_FRST_FLG, 'N') = 'N'
AND NVL(DELT_FLG, 'N') = 'N' AND CGO_TP_CD = 'F'			]]></sql>
			<params>
				<param name="cop_no" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
