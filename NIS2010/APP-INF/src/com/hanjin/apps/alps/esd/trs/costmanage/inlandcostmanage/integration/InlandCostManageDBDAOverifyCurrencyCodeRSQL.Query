<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="InlandCostManageDBDAOverifyCurrencyCodeRSQL">
			<desc><![CDATA[verifyCurrencyCode]]></desc>
			<sql><![CDATA[
SELECT DECODE(COUNT(CURR_CD) - $curr_cd_arr.size(),0,'N','Y') AS ERR_FLG
  FROM MDM_CURRENCY
 WHERE NVL(DELT_FLG, 'N') <> 'Y'

#if (${curr_cd_arr} != '')
AND CURR_CD IN (
    #foreach ($curr_cd_arr_num IN ${curr_cd_arr})
        #if($velocityCount < $curr_cd_arr.size())
          '$curr_cd_arr_num',
        #else
          '$curr_cd_arr_num'
        #end
    #end
        )
#end			]]></sql>
			<params>
			</params>
		</query>
	</querys>
</sqls>
