<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="CustomerNominatedTruckerRgstDAOSearchDupChkSpNameByScacCdRSQL">
			<desc><![CDATA[SCAC CODE로 VENDOR를 조회했을 경우 중복된 건이 있는지 체크한다.]]></desc>
			<sql><![CDATA[
SELECT CASE WHEN COUNT(A.VNDR_SEQ) > 1 THEN 'Y'
            WHEN COUNT(A.VNDR_SEQ) = 1 THEN 'N'
            WHEN COUNT(A.VNDR_SEQ) = 0 THEN 'A' 
        END AS DUP_CD
  FROM MDM_VENDOR A
 WHERE 1=1
   AND A.USA_EDI_CD = @[usa_edi_cd]
   AND A.DELT_FLG = 'N'			]]></sql>
			<params>
				<param name="usa_edi_cd" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
