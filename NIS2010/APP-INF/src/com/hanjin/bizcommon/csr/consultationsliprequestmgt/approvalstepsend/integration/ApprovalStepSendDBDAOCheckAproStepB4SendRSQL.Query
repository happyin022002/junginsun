<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="ApprovalStepSendDBDAOCheckAproStepB4SendRSQL">
			<desc><![CDATA[AproStep 대상 전송 전 DB에서의 전송 대기 가능 상태 조회]]></desc>
			<sql><![CDATA[
SELECT NVL((
SELECT L.EXE_STS_CD
FROM COM_APRO_SND_LOG L
WHERE 1=1
AND L.SND_LOG_SEQ = @[snd_log_seq]
),'E') EXE_STS_CD
FROM DUAL			]]></sql>
			<params>
				<param name="snd_log_seq" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
