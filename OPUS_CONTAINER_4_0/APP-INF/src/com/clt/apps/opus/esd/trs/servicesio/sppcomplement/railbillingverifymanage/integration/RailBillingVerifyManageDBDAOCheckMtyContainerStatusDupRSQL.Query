<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="RailBillingVerifyManageDBDAOCheckMtyContainerStatusDupRSQL">
			<desc><![CDATA[SEN, HJL, HJS RAIL BILLING 중복 CHECK]]></desc>
			<sql><![CDATA[
SELECT 
    EQ_NO,
    MAX(VERIFY_RESULT) VERIFY_RESULT,
    MAX(VERIFY_YN) VERIFY_YN,
    MAX(CXL_RQST) CXL_RQST
FROM 
    (
        SELECT EQ_NO,
            SUBSTR( TO_CHAR(LOCL_CRE_DT, 'YYYYMMDD') || 'NYK - CNTR_NO: ' 
            || EQ_NO || ' S/O created( '|| TO_CHAR(LOCL_CRE_DT, 'YYYYMMDD') || DECODE(TRSP_BND_CD, Null , ' : Mty )', ' : Full )') 
            || ' ROUTE : ' || FM_NOD_CD || '-->' || TO_NOD_CD , 9, 100) VERIFY_RESULT,
            CASE WHEN (SO.TRSP_BND_CD = 'O' OR TRSP_BND_CD IS NULL) 
                        AND NVL(SO.DELT_FLG , 'N') = 'N' THEN 'Y' 
                 WHEN SO.TRSP_BND_CD = 'I' AND NVL(SO.DELT_FLG , 'N') = 'N' THEN 'N' 
                 ELSE 'N' 
            END VERIFY_YN,
            DECODE(SIGN(TRUNC(GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') - SO.CXL_RQST_DT )-1 ), -1, 'Y', 0, 'Y', 'N') CXL_RQST
        FROM
            TRS_TRSP_RAIL_BIL_ORD SO
        WHERE ( NVL(DELT_FLG, 'N') = 'N' OR SO.CXL_RQST_DT IS NOT NULL )
        AND   EQ_NO = @[eqNo]
        AND   ( ( SO.TRSP_BND_CD = 'O' AND LOCL_CRE_DT BETWEEN (GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') - 15)
                     AND GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') )
                OR (SO.TRSP_BND_CD = 'I' AND LOCL_CRE_DT BETWEEN (GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') - 5) 
                    AND GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') )
                OR (SO.CGO_TP_CD = 'M' AND LOCL_CRE_DT BETWEEN (GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') - 15) 
                    AND GLOBALDATE_PKG.TIME_LOCAL_OFC_FNC('CHIBB') ) 
              )
	)
GROUP BY 
    EQ_NO			]]></sql>
			<params>
				<param name="eqNo" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
