<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="TerminalDepartureReportDBDAOsearchTotMoveDataRSQL">
			<desc><![CDATA[DESC Enter..]]></desc>
			<sql><![CDATA[
SELECT
NVL(
        SUM( NVL( FULL_BO_20, 0) )+-- FULL_BO_20 
        SUM( NVL( FULL_BO_2H, 0) )+-- FULL_BO_2H  
        SUM( NVL( FULL_BO_40, 0) )+-- FULL_BO_40 
        SUM( NVL( FULL_BO_4H, 0) )+-- FULL_BO_4H 
        SUM( NVL( FULL_BO_45, 0) )+-- FULL_BO_45 
        SUM( NVL( FULL_TS_20, 0) )+-- FULL_TS_20 
        SUM( NVL( FULL_TS_2H, 0) )+-- FULL_TS_2H 
        SUM( NVL( FULL_TS_40, 0) )+-- FULL_TS_40 
        SUM( NVL( FULL_TS_4H, 0) )+-- FULL_TS_4H 
        SUM( NVL( FULL_TS_45, 0) )+-- FULL_TS_45 
        SUM( NVL( ET_BO_20  , 0) )+--   ET_BO_20 
        SUM( NVL( ET_BO_2H  , 0) )+--   ET_BO_2H 
        SUM( NVL( ET_BO_40  , 0) )+--   ET_BO_40 
        SUM( NVL( ET_BO_4H  , 0) )+--   ET_BO_4H 
        SUM( NVL( ET_BO_45  , 0) )+--   ET_BO_45 
        SUM( NVL( ET_TS_20  , 0) )+--   ET_TS_20 
        SUM( NVL( ET_TS_2H  , 0) )+--   ET_TS_2H 
        SUM( NVL( ET_TS_40  , 0) )+--   ET_TS_40 
        SUM( NVL( ET_TS_4H  , 0) )+--   ET_TS_4H 
        SUM( NVL( ET_TS_45  , 0) )+
        MAX(RH_MISHAND.RH_MISHAND_CNT), 0)   TOT_MVS

FROM  (
       SELECT  
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, '2', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_BO_20,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, '3', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_BO_2H,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, '4', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_BO_40,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, 'H', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_BO_4H,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, 'L', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_BO_45,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, '2', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_TS_20,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, '3', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_TS_2H,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, '4', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_TS_40,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, 'H', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_TS_4H,
        SUM(DECODE(S.CNTR_TYPE, 'E', NULL, DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, 'L', DECODE(QTY,0, NULL, QTY), NULL), NULL))) FULL_TS_45,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, '2', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_BO_20,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, '3', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_BO_2H,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, '4', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_BO_40,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, 'H', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_BO_4H,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DS', DECODE(S.CNTR_SIZE, 'L', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_BO_45,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, '2', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_TS_20,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, '3', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_TS_2H,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, '4', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_TS_40,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, 'H', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_TS_4H,
        SUM(DECODE(S.CNTR_TYPE, 'E', DECODE(S.STATUS, 'DT', DECODE(S.CNTR_SIZE, 'L', DECODE(QTY,0, NULL, QTY), NULL), NULL), NULL)) ET_TS_45
        FROM   VSK_VSL_PORT_SKD V, TDR_HEADER H, TDR_SUMMARY S
        WHERE  V.VSL_CD  		= @[vsl_cd]
        AND    V.SKD_VOY_NO  	= @[voy_no]
        AND    V.SKD_DIR_CD  	= @[dir_cd]
        AND    V.YD_CD 			= @[yd_cd]
        AND    V.CLPT_IND_SEQ   = @[clpt_ind_seq]
        AND    V.VSL_CD       	= H.VSL_CD
        AND    V.SKD_VOY_NO   	= H.VOY_NO
        AND    V.SKD_DIR_CD   	= H.DIR_CD
        AND    V.VPS_PORT_CD  	= H.PORT_CD
        AND    V.CLPT_IND_SEQ 	= H.CALL_IND
        AND    H.VSL_CD       	= S.VSL_CD
        AND    H.VOY_NO       	= S.VOY_NO
        AND    H.DIR_CD       	= S.DIR_CD
        AND    H.PORT_CD      	= S.PORT_CD
        AND    H.CALL_IND     	= S.CALL_IND
        AND    S.STATUS   IN ('DS','DT')
        AND	   S.CNTR_TYPE NOT IN ('D', 'R', 'A')
        GROUP BY S.OPR_CD
        UNION ALL
        SELECT  
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_20,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_2H,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_40,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_4H,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_45,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_20,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_2H,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_40,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_4H,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_45,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_20,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_2H,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_40,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_4H,
        SUM(DECODE(STATUS, 'LM', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_45,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_20,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_2H,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_40,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_4H,
        SUM(DECODE(STATUS, 'OT', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_45

        FROM   VSK_VSL_PORT_SKD V, TDR_HEADER H, TDR_SUMMARY S
        WHERE  V.VSL_CD  		= @[vsl_cd]
        AND    V.SKD_VOY_NO  	= @[voy_no]
        AND    V.SKD_DIR_CD  	= @[dir_cd]
        AND    V.YD_CD 			= @[yd_cd]
        AND    V.CLPT_IND_SEQ   = @[clpt_ind_seq]
        AND    S.STATUS IN ('LM', 'OT')
        AND    S.CNTR_TYPE NOT IN ('D', 'R', 'A')
        AND    V.VSL_CD       	= H.VSL_CD
        AND    V.SKD_VOY_NO   	= H.VOY_NO
        AND    V.SKD_DIR_CD   	= H.DIR_CD
        AND    V.VPS_PORT_CD  	= H.PORT_CD
        AND    V.CLPT_IND_SEQ 	= H.CALL_IND
        AND    H.VSL_CD       	= S.VSL_CD
        AND    H.VOY_NO       	= S.VOY_NO
        AND    H.DIR_CD       	= S.DIR_CD
        AND    H.PORT_CD      	= S.PORT_CD
        AND    H.CALL_IND     	= S.CALL_IND
        GROUP BY OPR_CD,  POD
        UNION ALL
        SELECT  
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_20,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_2H,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_40,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_4H,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_BO_45,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_20,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_2H,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_40,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_4H,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'F', DECODE(QTY, 0, NULL, QTY), 'R', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))      				FULL_TS_45,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_20,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_2H,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_40,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_4H,
        SUM(DECODE(STATUS, 'LI', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_BO_45,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, '2', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_20,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, '3', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_2H,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, '4', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_40,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, 'H', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_4H,
        SUM(DECODE(STATUS, 'LT', DECODE(CNTR_SIZE, 'L', DECODE(CNTR_TYPE, 'E', DECODE(QTY, 0, NULL, QTY), NULL), NULL), NULL))                										ET_TS_45
        FROM   VSK_VSL_PORT_SKD V, TDR_HEADER H, TDR_SUMMARY S
        WHERE  V.VSL_CD  		= @[vsl_cd]
        AND    V.SKD_VOY_NO  	= @[voy_no]
        AND    V.SKD_DIR_CD  	= @[dir_cd]
        AND    V.YD_CD 			= @[yd_cd]
        AND    V.CLPT_IND_SEQ   = @[clpt_ind_seq]
        AND    S.STATUS IN ('LI', 'LT')
        AND    S.CNTR_TYPE NOT IN ('D', 'R', 'A')
        AND    V.VSL_CD       	= H.VSL_CD
        AND    V.SKD_VOY_NO   	= H.VOY_NO
        AND    V.SKD_DIR_CD   	= H.DIR_CD
        AND    V.VPS_PORT_CD  	= H.PORT_CD
        AND    V.CLPT_IND_SEQ 	= H.CALL_IND
        AND    H.VSL_CD       	= S.VSL_CD
        AND    H.VOY_NO       	= S.VOY_NO
        AND    H.DIR_CD       	= S.DIR_CD
        AND    H.PORT_CD      	= S.PORT_CD
        AND    H.CALL_IND     	= S.CALL_IND
        GROUP BY OPR_CD,  POD
       ),
(SELECT
         SUM(RH_MISHAND_SUB_SUM.CNT) RH_MISHAND_CNT
 FROM
    (/*MISHANDLE*/
        SELECT 
               COUNT(*) CNT
        FROM   VSK_VSL_PORT_SKD V, TDR_HEADER H, TDR_CNTR_DETAIL C
        WHERE  V.VSL_CD  		= @[vsl_cd]
        AND    V.SKD_VOY_NO  	= @[voy_no]
        AND    V.SKD_DIR_CD  	= @[dir_cd]
        AND    V.YD_CD 			= @[yd_cd]
        AND    V.CLPT_IND_SEQ   = @[clpt_ind_seq]
        AND    V.VSL_CD       	= H.VSL_CD
        AND    V.SKD_VOY_NO   	= H.VOY_NO
        AND    V.SKD_DIR_CD   	= H.DIR_CD
        AND    V.VPS_PORT_CD  	= H.PORT_CD
        AND    V.CLPT_IND_SEQ 	= H.CALL_IND
        AND    H.VSL_CD       	= C.VSL_CD
        AND    H.VOY_NO       	= C.VOY_NO
        AND    H.DIR_CD       	= C.DIR_CD
        AND    H.PORT_CD      	= C.PORT_CD
        AND    H.CALL_IND     	= C.CALL_IND
        AND    C.STATUS        	= 'MI'
        AND    C.MISHANDLE_CHK  IN ( 'SD', 'OD','SL','OL')
        UNION ALL
        SELECT /* R/H  */
               NVL( SUM( DECODE(C.SHIFT_TYPE, 'Q', 2, 1 )  ), 0) CNT
        FROM   VSK_VSL_PORT_SKD V, TDR_HEADER H, TDR_CNTR_DETAIL C
        WHERE  V.VSL_CD  		= @[vsl_cd]
        AND    V.SKD_VOY_NO  	= @[voy_no]
        AND    V.SKD_DIR_CD  	= @[dir_cd]
        AND    V.YD_CD 			= @[yd_cd]
        AND    V.CLPT_IND_SEQ   = @[clpt_ind_seq]
        AND    V.VSL_CD       	= H.VSL_CD
        AND    V.SKD_VOY_NO   	= H.VOY_NO
        AND    V.SKD_DIR_CD   	= H.DIR_CD
        AND    V.VPS_PORT_CD  	= H.PORT_CD
        AND    V.CLPT_IND_SEQ 	= H.CALL_IND
        AND    H.VSL_CD       	= C.VSL_CD
        AND    H.VOY_NO       	= C.VOY_NO
        AND    H.DIR_CD       	= C.DIR_CD
        AND    H.PORT_CD      	= C.PORT_CD
        AND    H.CALL_IND     	= C.CALL_IND
        AND    C.STATUS         = 'ST'
        AND    TRIM(C.PRECELL) IS NOT NULL
    ) RH_MISHAND_SUB_SUM
)RH_MISHAND			]]></sql>
			<params>
				<param name="vsl_cd" type="12" value="" out="N"/>
				<param name="voy_no" type="12" value="" out="N"/>
				<param name="dir_cd" type="12" value="" out="N"/>
				<param name="yd_cd" type="12" value="" out="N"/>
				<param name="clpt_ind_seq" type="12" value="" out="N"/>
			</params>
		</query>
	</querys>
</sqls>
