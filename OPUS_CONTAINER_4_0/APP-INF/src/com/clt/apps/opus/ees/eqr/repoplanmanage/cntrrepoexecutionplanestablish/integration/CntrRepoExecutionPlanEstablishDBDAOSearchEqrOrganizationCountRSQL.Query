<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<querys>
		<query name="CntrRepoExecutionPlanEstablishDBDAOSearchEqrOrganizationCountRSQL">
			<desc><![CDATA[MDM_EQ_ORZ_CHT 테이블에서 RCC, LCC, ECC, SCC COUNT를 조회]]></desc>
			<sql><![CDATA[
SELECT	 COUNT(DISTINCT RCC_CD) AS RCC_CD
		,COUNT(DISTINCT LCC_CD) AS LCC_CD
		,COUNT(DISTINCT ECC_CD) AS ECC_CD
		,COUNT(DISTINCT SCC_CD) AS SCC_CD
FROM	MDM_EQ_ORZ_CHT
WHERE	DELT_FLG = 'N'			]]></sql>
			<params>
			</params>
		</query>
	</querys>
</sqls>
